export const environment = {
  production: false,
  // In dev, Angular dev-server proxies /api to the ASP.NET API (see proxy.conf.json).
  apiUrl: '/api',
  apiUrl: '' // Empty string for relative URLs in development
};

