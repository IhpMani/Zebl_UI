/***************************************************************************************************
 * Load `zone.js` for the application. By default, Zone.js will be included in production builds only.
 * To include it in development as well, add it to the `polyfills` array in angular.json.
 */
import 'zone.js';  // Included with Angular CLI.