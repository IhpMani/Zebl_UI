import { Component } from '@angular/core';

@Component({
  selector: 'app-workspace-host',
  templateUrl: './workspace-host.component.html',
  styleUrls: ['./workspace-host.component.css']
})
export class WorkspaceHostComponent {
  // Host is now a simple container for the tabs + routed content.
}

